import { PageButtonDirective } from './page-button.directive';

describe('PageButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PageButtonDirective();
    expect(directive).toBeTruthy();
  });
});
