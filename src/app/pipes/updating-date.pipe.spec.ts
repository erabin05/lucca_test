import { UpdatingDatePipe } from './updating-date.pipe';

describe('UpdatingDatePipe', () => {
  it('create an instance', () => {
    const pipe = new UpdatingDatePipe();
    expect(pipe).toBeTruthy();
  });
});
